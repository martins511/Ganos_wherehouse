{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migration": "npx sequelize migration:generate --name",
    "migrate:specific": "npx sequelize-cli db:migrate --to",
    "migrate": "npx sequelize db:migrate",
    "migrate:rollback": "npx sequelize db:migrate:undo",
    "drop:migrate": "npx sequelize db:migrate:undo --name",
    "model": "npx sequelize-cli model:generate --name Capital --attributes",
    "create:db": "npx sequelize-cli db:create",
    "drop:db": "npx sequelize-cli db:drop",
    "start": "nodemon index"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mysql2": "^3.7.0",
    "nodemon": "^3.0.2",
    "sequelize": "^6.35.2",
    "sequelize-cli": "^6.6.2"
  }
}
